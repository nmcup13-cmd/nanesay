// ─────────────────────────────────────────────────────────────
// js/ai-config.example.js — แม่แบบ (ไม่มีคีย์จริง) — ไฟล์นี้ขึ้น GitHub ได้
//
// วิธีใช้:
// 1. คัดลอกไฟล์นี้เป็น js/ai-config.js (ชื่อไฟล์นั้นถูกกันไว้ใน .gitignore แล้ว)
// 2. ใส่คีย์ของหลักสูตร (ที่ผู้สอนตั้งวงเงินไว้) แทนที่ "ใส่คีย์ของหลักสูตรตรงนี้"
// 3. ห้ามใช้คีย์ส่วนตัว — ใช้คีย์ของหลักสูตรเท่านั้น
// ─────────────────────────────────────────────────────────────

export const OPENROUTER_API_KEY = "ใส่คีย์ของหลักสูตรตรงนี้";
export const OPENROUTER_MODEL = "google/gemini-2.5-flash-lite";
