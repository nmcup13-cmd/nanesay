# ACL.md — ตารางสิทธิ์ผู้ใช้ 3 บทบาท (LeaveEasy)

อ้างอิงจาก `leaveeasy-spec.md` หัวข้อ 2 "ผู้ใช้ 3 บทบาท" และเกณฑ์การยอมรับของ US-04 ถึง US-08

> ⚠️ **สถานะการบังคับใช้จริงในโค้ดตอนนี้**: กฎขั้นต่ำ "ต้องล็อกอินก่อน ถึงอ่าน/เขียนได้" deploy แล้ว
> (สัปดาห์ที่ 7) แต่ยัง**ไม่ได้แยกสิทธิ์ตาม role** ใน Firestore Security Rules จริง —
> ทุกคนที่ล็อกอินอยู่ตอนนี้ทำได้เหมือนกันหมดไม่ว่า role จะเป็นอะไร การแยกสิทธิ์ตามตารางนี้เป็นงาน
> สัปดาห์ที่ 8

| บทบาท (`role`) | ทำได้ | ทำไม่ได้ |
|---|---|---|
| 🙋 **ผู้ขอลา** (`employee`) | • ยื่นใบขอลาใหม่<br>• ดูใบลาของตัวเอง<br>• เขียนความเห็นเพิ่มในใบของตัวเอง<br>• ลบใบของตัวเองที่สถานะยังเป็น `รอพิจารณา` | • **อนุมัติ/ไม่อนุมัติใบลาของตัวเอง** (ไม่มีสิทธิ์เปลี่ยนสถานะใบลาเลย ไม่ว่าใบใคร)<br>• **ดูใบลาของคนอื่น**<br>• แก้ไขประเภทการลา<br>• ลบใบที่ไม่ใช่ของตัวเอง หรือใบที่พิจารณาแล้ว |
| ✅ **ผู้อนุมัติ** (`manager`) | • ดูใบลาทุกใบ<br>• เปลี่ยนสถานะใบลา (อนุมัติ/ไม่อนุมัติ — แก้ได้เฉพาะช่อง `status` เท่านั้น)<br>• เขียนความเห็นในทุกใบ | • **อนุมัติ/ไม่อนุมัติใบลาของตัวเอง** แม้จะมีสิทธิ์อนุมัติใบของคนอื่นก็ตาม (กันผลประโยชน์ทับซ้อน)<br>• ลบใบลาของคนอื่น<br>• แก้ไขประเภทการลา |
| 🧾 **ฝ่ายบุคคล** (`hr`) | • ทำได้ทุกอย่างที่ผู้อนุมัติทำได้<br>• กำหนดผู้อนุมัติให้แต่ละใบ<br>• เพิ่ม/แก้/ลบ ประเภทการลา<br>• เห็นแดชบอร์ดสรุปทั้งระบบ | • **อนุมัติ/ไม่อนุมัติใบลาของตัวเอง** เช่นเดียวกับผู้อนุมัติ (กันผลประโยชน์ทับซ้อน)<br>• ลบใบลาของคนอื่น (ลบได้เฉพาะประเภทการลา ไม่ใช่ใบลา) |

## หมายเหตุ

- ค่าที่เก็บจริงในช่อง `role` ของเอกสาร Firestore ต้องเป็นรหัสภาษาอังกฤษเสมอ (`employee` /
  `manager` / `hr`) — ใช้คำไทยเฉพาะตอนแสดงผลบนหน้าจอเท่านั้น (ตามข้อกำหนดในสเปก)
- คนที่ไม่ได้ล็อกอินเลย **อ่านข้อมูลอะไรไม่ได้ทั้งหมด** ไม่ว่า role ใด (บังคับใช้จริงแล้วตั้งแต่
  สัปดาห์ที่ 7)
- "ผู้ขอลาเห็นเฉพาะใบของตัวเอง" และกฎเฝ้าข้อมูลแยกตาม role ข้างต้น (รวมโฟลเดอร์ย่อย `approvals`)
  ยังไม่ได้ implement จริง — เป็นงานสัปดาห์ที่ 8 ตาม US-08
- กฎ **"ห้ามอนุมัติใบลาของตัวเอง"** สำหรับผู้อนุมัติและฝ่ายบุคคล เป็นข้อกำหนดป้องกันผลประโยชน์ทับซ้อน
  ที่เพิ่มเข้ามาตามการตรวจทาน ไม่ได้เขียนไว้ตรงๆ เป็นข้อแยกในสเปกต้นฉบับ (`leaveeasy-spec.md`) —
  ตอน implement จริงในสัปดาห์ที่ 8 ต้องเขียนกฎนี้ไว้ใน Security Rules ด้วย ไม่ใช่แค่ในเอกสารนี้
